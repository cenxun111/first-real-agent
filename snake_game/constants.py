#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
贪吃蛇游戏 - 常量定义模块
集中管理所有全局常量，便于维护和配置
"""

# ==================== 屏幕尺寸 ====================
SCREEN_WIDTH = 640      # 屏幕宽度（像素）
SCREEN_HEIGHT = 480     # 屏幕高度（像素）

# ==================== 网格设置 ====================
GRID_SIZE = 20          # 网格大小（像素），决定蛇身和食物的显示粒度
GRID_COUNT_X = SCREEN_WIDTH // GRID_SIZE   # X 方向网格数量
GRID_COUNT_Y = SCREEN_HEIGHT // GRID_SIZE  # Y 方向网格数量

# ==================== 游戏速度 ====================
INITIAL_SPEED = 15      # 初始移动速度（帧/秒）
SPEED_INCREMENT = 0.2   # 每吃一个食物增加的速度
MAX_SPEED = 30          # 最大速度限制

# ==================== 蛇的初始状态 ====================
STARTING_LENGTH = 3     # 蛇的初始长度
INITIAL_DIRECTION = (0, -1)  # 初始移动方向：向上（dx=0, dy=-1）

# ==================== 颜色定义 ====================
COLORS = {
    'background': (20, 20, 25),          # 深灰背景
    'snake_head': (40, 200, 180),        # 蛇头：青色渐变
    'snake_body': (30, 160, 140),        # 蛇身：青绿色
    'food': (255, 100, 100),             # 食物：红色
    'text': (255, 255, 255),             # 文字：白色
    'score': (255, 255, 200),            # 分数：淡黄色
    'high_score': (255, 180, 100),       # 最高分：橙色
}

# ==================== 游戏状态 ====================
GAME_STATE = {
    'running': False,      # 游戏运行中
    'paused': False,       # 暂停状态
    'game_over': False,    # 游戏结束
}

# ==================== 按键控制 ====================
KEY_UP = (0, -1)          # 向上移动
KEY_DOWN = (0, 1)         # 向下移动
KEY_LEFT = (-1, 0)        # 向左移动
KEY_RIGHT = (1, 0)        # 向右移动

# ==================== 字体设置 ====================
FONT_SIZE = 24            # 主字体大小
SCORE_FONT_SIZE = 36      # 分数显示字体大小
TITLE_FONT_SIZE = 48      # 标题字体大小
